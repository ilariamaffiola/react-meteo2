# Example environment variables for react-meteo (do NOT store real secrets here)
# Copy this to `.env` and fill in your real keys locally.

# Example keys (use VITE_ or REACT_APP_ prefixes depending on your setup)
VITE_WEATHER_API_KEY=your_api_key_here
REACT_APP_MAPS_KEY=your_maps_key_here

# Add other environment variables used by your app
